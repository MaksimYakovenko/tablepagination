import { TranslateHeadersPipe } from './translate-headers.pipe';

describe('TranslateHeadersPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateHeadersPipe();
    expect(pipe).toBeTruthy();
  });
});
